#include <stdio.h>
#include <string.h>

int main()
{
  char txt[50];
  int len = 0;
  int a=0, e=0, i=0, o=0, u=0;
  printf("Enter word:");
  scanf("%s", txt);
  printf("%s", txt);

	while (txt[len] != '\0')
	{
		len++;
	}

  for (i=0; i < len; i++) {
    
  }
}